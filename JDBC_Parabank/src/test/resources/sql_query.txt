Create database testdb;
use testdb;

create table users(
	firstname varchar(50), 
    lastname varchar(50),
    address VARCHAR(100), 
    city VARCHAR(50), 
    state VARCHAR(50), 
    zip VARCHAR(20), 
    phone VARCHAR(20), 
    ssn VARCHAR(20), 
    username VARCHAR(50), 
    password VARCHAR(50), 
    confirm VARCHAR(50)
);

INSERT INTO users VALUES ('John', 'Doe', '123 St', 'LA', 'CA', '90001', '1234567890', '12345', 'johndoe1', 'pass123', 'pass123');

select * from users;
